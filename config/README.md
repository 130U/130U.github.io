# Tool Configuration

- `eslint.config.mjs` — repository code-quality rules.

The ESLint command points here explicitly. Next.js and TypeScript continue to
use their standard root configuration files.
