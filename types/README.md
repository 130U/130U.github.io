# Shared Type Declarations

No repository-wide ambient declaration is currently required. Keep future
shared declarations here only when a dependency or non-TypeScript import cannot
be typed beside its owning module; the archived Markdown record is read through
Node's filesystem API and needs no raw-module declaration.
