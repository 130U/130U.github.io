# Current Chapter

Public label: **Current Chapter**

Public URL:

```text
/now/
```

Page source:

```text
app/now/page.tsx
```

The directory remains named `now` because route directories define public URLs.
Do not rename it to `current-chapter` without an intentional URL migration and
redirect plan.
